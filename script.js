let selected = null; const card = document.getElementById("card"); const defaultText = document.querySelector(".text-box"); selectElement(defaultText); enableDrag(defaultText); enableResize(defaultText); /* -------------------- SELECT ELEMENT -------------------- */ function selectElement(element) { document.querySelectorAll(".text-box, .image-box") .forEach(el => el.classList.remove("selected")); selected = element; selected.classList.add("selected"); updateToolbar(); } /* -------------------- ADD TEXT -------------------- */ document.getElementById("addTextBtn") .addEventListener("click", () => { const text = document.createElement("div"); text.className = "text-box"; text.contentEditable = true; text.innerHTML = ` Double Click To Edit <div class="resize-handle"></div> `; text.style.left = "50px"; text.style.top = "80px"; text.style.fontSize = "22px"; card.appendChild(text); enableDrag(text); enableResize(text); selectElement(text); }); /* -------------------- ADD IMAGE -------------------- */ document.getElementById("addImageBtn") .addEventListener("click", () => { const input = document.createElement("input"); input.type = "file"; input.accept = "image/*"; input.click(); input.onchange = () => { const file = input.files[0]; if (!file) return; const reader = new FileReader(); reader.onload = (e) => { const img = document.createElement("img"); img.src = e.target.result; img.className = "image-box"; img.style.left = "50px"; img.style.top = "150px"; img.style.width = "150px"; const wrapper = document.createElement("div"); wrapper.className = "image-wrapper"; wrapper.style.position = "absolute"; wrapper.style.left = img.style.left; wrapper.style.top = img.style.top; wrapper.appendChild(img); const resizeHandle = document.createElement("div"); resizeHandle.className = "resize-handle"; wrapper.appendChild(resizeHandle); card.appendChild(wrapper); enableDrag(wrapper); enableResize(wrapper); wrapper.addEventListener("click", () => { selectElement(wrapper); }); selectElement(wrapper); }; reader.readAsDataURL(file); }; }); /* -------------------- DRAG -------------------- */ function enableDrag(element) { element.addEventListener("mousedown", (e) => { if (e.target.classList.contains("resize-handle")) return; selectElement(element); let startX = e.clientX; let startY = e.clientY; let initialX = parseInt(element.style.left || 0); let initialY = parseInt(element.style.top || 0); function move(ev) { element.style.left = initialX + (ev.clientX - startX) + "px"; element.style.top = initialY + (ev.clientY - startY) + "px"; } function stop() { window.removeEventListener("mousemove", move); window.removeEventListener("mouseup", stop); } window.addEventListener("mousemove", move); window.addEventListener("mouseup", stop); }); element.addEventListener("click", () => { selectElement(element); }); } /* -------------------- RESIZE -------------------- */ function enableResize(element) { const handle = element.querySelector(".resize-handle"); if (!handle) return; handle.addEventListener("mousedown", (e) => { e.stopPropagation(); selectElement(element); let startX = e.clientX; let startWidth = element.offsetWidth; let startFont = parseFloat(window.getComputedStyle(element).fontSize) || 22; function resize(ev) { let diff = ev.clientX - startX; /* Text Resize */ if (element.classList.contains("text-box")) { element.style.fontSize = Math.max(12, startFont + diff * 0.2) + "px"; } /* Image Resize */ else { element.style.width = Math.max(100, startWidth + diff) + "px"; } updateToolbar(); } function stopResize() { window.removeEventListener("mousemove", resize); window.removeEventListener("mouseup", stopResize); } window.addEventListener("mousemove", resize); window.addEventListener("mouseup", stopResize); }); } /* -------------------- TOOLBAR -------------------- */ function updateToolbar() { if (!selected) return; if (selected.classList.contains("text-box")) { document.getElementById("fontSize").value = parseInt(selected.style.fontSize) || 22; document.getElementById("fontFamily").value = selected.style.fontFamily || "Arial"; document.getElementById("textColor").value = rgbToHex(selected.style.color || "#000000"); document.getElementById("opacityRange").value = selected.style.opacity || 1; } } /* -------------------- INPUTS -------------------- */ document.getElementById("fontSize") .oninput = (e) => { if (selected?.classList.contains("text-box")) { selected.style.fontSize = e.target.value + "px"; } }; document.getElementById("fontFamily") .onchange = (e) => { if (selected?.classList.contains("text-box")) { selected.style.fontFamily = e.target.value; } }; document.getElementById("textColor") .oninput = (e) => { if (selected?.classList.contains("text-box")) { selected.style.color = e.target.value; } }; document.getElementById("letterSpacing") .oninput = (e) => { if (selected?.classList.contains("text-box")) { selected.style.letterSpacing = e.target.value + "px"; } }; document.getElementById("lineHeight") .oninput = (e) => { if (selected?.classList.contains("text-box")) { selected.style.lineHeight = e.target.value; } }; document.getElementById("opacityRange") .oninput = (e) => { if (selected) { selected.style.opacity = e.target.value; } }; document.getElementById("alignSelect") .onchange = (e) => { if (selected?.classList.contains("text-box")) { selected.style.textAlign = e.target.value; } }; /* -------------------- TEXT STYLE -------------------- */ function toggleStyle(property, value) { if (!selected?.classList.contains("text-box")) return; selected.style[property] = selected.style[property] === value ? "" : value; } document.getElementById("boldBtn") .onclick = () => { toggleStyle("fontWeight", "bold"); }; document.getElementById("italicBtn") .onclick = () => { toggleStyle("fontStyle", "italic"); }; document.getElementById("underlineBtn") .onclick = () => { toggleStyle("textDecoration", "underline"); }; /* -------------------- DELETE -------------------- */ document.getElementById("deleteBtn") .onclick = () => { if (selected && confirm("Delete selected item?")) { selected.remove(); selected = null; } }; /* -------------------- TEMPLATE CHANGE -------------------- */ document.getElementById("templateSelect") .addEventListener("change", (e) => { card.className = "card"; card.classList.add(e.target.value); }); /* -------------------- DOWNLOAD PNG -------------------- */ document.getElementById("downloadBtn") .addEventListener("click", () => { html2canvas(card).then(canvas => { const link = document.createElement("a"); link.download = "invitation.png"; link.href = canvas.toDataURL("image/png"); link.click(); }); }); /* -------------------- RGB TO HEX -------------------- */ function rgbToHex(rgb) { if (rgb.startsWith("#")) return rgb; const result = rgb.match(/\d+/g); if (!result) return "#000000"; return "#" + result.slice(0, 3) .map(x => parseInt(x) .toString(16) .padStart(2, "0")) .join(""); } /* -------------------- KEYBOARD SHORTCUTS -------------------- */ document.addEventListener("keydown", (e) => { if (e.ctrlKey && e.key === "b") { e.preventDefault(); toggleStyle("fontWeight", "bold"); } if (e.ctrlKey && e.key === "i") { e.preventDefault(); toggleStyle("fontStyle", "italic"); } if (e.key === "Delete" && selected) { selected.remove(); selected = null; } });
